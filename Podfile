# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'azenia' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  pod 'Alamofire', '~> 5.2'
  pod 'HandyJSON', '~> 5.0.2'
  pod 'SDWebImage', '~> 5.0'
  pod 'SnapKit', '~> 5.0.0'

  target 'azeniaTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'azeniaUITests' do
    # Pods for testing
  end

end
